name: mcp_sample
publish_to: none
description: Genkit MCP testapp with both a stdio server and a host/client
resolution: workspace

environment:
  sdk: ^3.12.0

dependencies:
  genkit: ^0.17.0
  genkit_google_genai: ^0.4.0
  genkit_mcp: ^0.4.0
  schemantic: ^0.2.3

dev_dependencies:
  build_runner: ^2.10.5
  schemantic_builder: ^0.1.3
