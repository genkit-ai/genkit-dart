name: mcp_server_sample
publish_to: none
description: Genkit MCP stdio server testapp
resolution: workspace

environment:
  sdk: ^3.10.0

dependencies:
  genkit: ^0.13.0
  genkit_mcp: ^0.1.5
  schemantic: ^0.1.2

dev_dependencies:
  build_runner: ^2.10.5
