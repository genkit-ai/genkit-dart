name: mcp_server_sample
publish_to: none
description: Genkit MCP stdio server testapp
resolution: workspace

environment:
  sdk: ^3.10.0

dependencies:
  genkit: ^0.11.1
  genkit_mcp: ^0.1.2
  schemantic: ^0.1.0

dev_dependencies:
  build_runner: ^2.10.5
