name: mcp_server_sample
publish_to: none
description: Genkit MCP stdio server testapp
resolution: workspace

environment:
  sdk: ^3.10.0

dependencies:
  genkit: ^0.10.0-dev.18
  genkit_mcp: ^0.1.0-dev.0
  schemantic: ^0.1.0-dev.0

dev_dependencies:
  build_runner: ^2.10.5
