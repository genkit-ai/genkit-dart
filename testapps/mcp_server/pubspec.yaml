name: mcp_server_sample
publish_to: none
description: Genkit MCP stdio server testapp
resolution: workspace

environment:
  sdk: ^3.10.0

dependencies:
  genkit: ^0.13.2
  genkit_mcp: ^0.1.7
  schemantic: ^0.2.0-rc.0

dev_dependencies:
  build_runner: ^2.10.5
  schemantic_builder: ^0.1.0-rc.0
