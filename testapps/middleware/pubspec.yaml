name: middleware_sample
description: Genkit Middleware test app
publish_to: none
resolution: workspace
environment:
  sdk: ^3.12.0

dependencies:
  genkit: ^0.17.0-rc.1
  genkit_google_genai: ^0.4.0-rc.1
  genkit_middleware: ^0.7.0-rc.1
  schemantic: ^0.2.3-rc.1

dev_dependencies:
  build_runner: ^2.10.5
  test: ^1.29.0
