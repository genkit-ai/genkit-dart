name: agentic_patterns
description: Agentic patterns samples for Genkit Dart
publish_to: none

environment:
  sdk: ^3.12.0
resolution: workspace

dependencies:
  args: ^2.6.0
  genkit: ^0.17.0-rc.2
  genkit_google_genai: ^0.4.0-rc.2
  schemantic: ^0.2.3-rc.1

dev_dependencies:
  build_runner: ^2.10.5
  schemantic_builder: ^0.1.3-rc.1
