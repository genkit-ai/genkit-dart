name: openai_sample
publish_to: none
description: Genkit Dart OpenAI test application
resolution: workspace

environment:
  sdk: ^3.10.0

dependencies:
  genkit: ^0.10.1
  genkit_openai: ^0.2.0
  schemantic: ^0.1.0

dev_dependencies:
  build_runner: ^2.10.5
  test: ^1.29.0
