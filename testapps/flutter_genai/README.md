# flutter_genai

A sample Flutter application demonstrating how to use the Genkit Dart SDK to build a recipe generator using local generation, remote models, and server flows. Features include tool calling (e.g. `checkPantry`) and streaming generation.
