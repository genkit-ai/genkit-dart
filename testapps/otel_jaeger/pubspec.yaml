name: otel_jaeger_sample
publish_to: none
description: Genkit Dart OpenTelemetry sample exporting traces to Jaeger and metrics to a local collector (no Docker).
resolution: workspace

environment:
  sdk: ^3.12.0

dependencies:
  dartastic_opentelemetry: ^0.11.0
  genkit: ^0.17.0
  genkit_google_genai: ^0.4.0
  genkit_otel: ^0.1.0

dev_dependencies:
  test: ^1.29.0
