name: schemantic
description: A lightweight runtime API for type-safe data classes and JSON schemas. Use schemantic_builder as a dev dependency to generate code.
repository: https://github.com/genkit-ai/genkit-dart
license: Apache-2.0
resolution: workspace
version: 0.1.3
environment:
  sdk: ^3.10.0

dependencies:
  json_schema_builder: ^0.1.3

dev_dependencies:
  schemantic_builder: ^0.1.0
  test: ^1.29.0
