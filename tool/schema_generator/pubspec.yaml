name: schema_generator
description: A generator that creates Dart classes from a JSON schema.
version: 1.0.0
environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  build: ^2.4.1
  source_gen: ^1.5.0
  http: ^1.2.1
  code_builder: ^4.10.0
  dart_style: ^2.3.4
  json_annotation: ^4.9.0

dev_dependencies:
  lints: ^3.0.0
